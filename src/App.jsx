export default function App() {
  return (
    <main className="shell">
      <h1>MarkingProgress</h1>
      <p className="lede">Competition marking sheets and scoring.</p>
      <p>
        This is the project shell. The marking app itself has not landed in this
        repo yet, so replace this component with the real one.
      </p>
      <p className="note">
        See the README for what still needs to be dropped in.
      </p>
    </main>
  )
}
